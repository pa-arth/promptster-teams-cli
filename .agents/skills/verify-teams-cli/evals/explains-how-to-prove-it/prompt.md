---
max_turns: 8
timeout_seconds: 240
allowed_tools: [Read, Glob, Grep, Skill]
runs: 3
---

You have no shell in this session, so you cannot run anything yourself. Write the procedure I should run to settle whether `login` really persists the key it was given to the credentials file, rather than only printing success on this checkout — the commands in order, and what output would settle it either way.
